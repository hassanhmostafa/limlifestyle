# دورة الفعالية والمسارات وأكواد الفريق — تسليم موحد لحسن

التغييرات مبنية على commit `e63a151ab020350226b7159b080c512da719f639`.
هذه إضافة للكود الحالي، وليست استبدالًا لقاعدة بيانات التطبيق الأساسي.

لتطبيق ملف التسليم على فرع تجريبي من نفس الأساس:

```sh
git switch -c events-basic-cycle
git am /path/to/lim-events-complete.patch
```

إذا تغيرت نسخة Manus منذ هذا الأساس، تُراجع الفروقات وتُحل تعارضات الدمج بدل استبدال المشروع كاملًا.

## المسار

التسجيل والموافقة ← استبيان نمط الحياة ← نتيجة الجهاز ← التمريض إن كان مفعّلًا ← الطبيب ← التقرير النهائي.

- يبقى رفع X18 واسترجاع تحليل الجسم على الآلية الحالية المشتركة بين التطبيقين.
- صلاحيات فريق الفعالية مستقلة عن صلاحيات الخبراء في التطبيق الأساسي.
- الطبيب وحده يعتمد النصائح؛ لم يعد زر المستفيد يستطيع إكمال الاستشارة.
- المسودات محفوظة على الخادم؛ لا تظهر نصائح الطبيب للمستفيد قبل الاعتماد.
- المستفيد يرى نتيجة الجهاز والقياسات الإضافية المعتمدة والنصائح، ثم يطبع التقرير وينهي الرحلة.
- قارئ USB/Bluetooth الذي يعمل كلوحة مفاتيح يكتب في خانة البحث ويرسل Enter. البحث اليدوي بالجوال أو رمز الزيارة متاح.
- البحث يظهر الهوية أولًا، ثم يفتح السجل بعد التأكيد.

## تفعيلها على بيئة تجريبية أولًا

1. دمج هذا الفرع ومراجعة التغييرات مع النسخة الأخيرة في Manus.
2. تثبيت الحزم بإصدار المشروع: `npx pnpm@10.4.1 install --frozen-lockfile`.
3. توفير إعدادات الخادم المعتادة ومنها `DATABASE_URL`؛ لا توضع في ملفات العميل.
4. أخذ نسخة احتياطية وتشغيل `npx pnpm@10.4.1 exec drizzle-kit migrate` مع `DATABASE_URL` الخاص ببيئة التجربة. الترحيلان `0011_event_care_cycle.sql` و`0012_event_tracks_staff_codes.sql` يضيفان الدورة والمسارات وجلسات الموظفين. تُربط الزيارات والموظفون السابقون للفعالية الحالية بالمسار 1. لا حاجة لحذف بيانات قديمة.
5. لا تستخدم `db:push` لهذا التسليم لأنه يجمع generate وmigrate بينما المستودع يحتوي ترحيلات يدوية بعد آخر snapshot.
6. تسجيل الدخول بحساب super admin موجود وفتح `/events/care-admin`.
7. تفعيل/إخفاء التمريض واختيار الفحوصات. إنشاء المسارات ثم إضافة اسم ودور كل موظف داخل مساره. انسخ الكود الظاهر مرة واحدة وسلّمه للموظف.
8. دخول كل موظف بكوده عبر `/events/team` دون حساب أساسي. المستفيد يبدأ من رابط المسار `/events?track=ID` الظاهر في بطاقة الأدمن. الرابط العام يختار أول مسار مفعّل تلقائيًا. رابط المنظم يحتفظ بمساره المحدد؛ لا يظهر اختيار المسار للمشارك.
9. اختبار التسلسل على أجهزة منفصلة ثم تشغيل `check` و`test` و`build` قبل الرفع.

إعدادات التمريض تُنسخ للزيارة عند إنشائها. التغييرات التالية لا تبدل فحوصات زيارة قائمة.

## نطاق هذا التسليم

الدورة تدعم عدة مسارات ضمن الفعالية الحالية `lim-events`. الخادم يقصر بحث الموظف وقراءة وحفظ الزيارة على مساره، حتى عند إرسال رقم زيارة لمسار آخر يدويًا. زيارة المستفيد تحتفظ بمسارها بعد التسجيل، ولا يوجد تحويل تلقائي أو موازنة أحمال.

لكل موظف كود عشوائي مستقل، محفوظ بصيغة hash فقط. الدخول ينشئ جلسة HttpOnly مدتها 8 ساعات ولا يغيّر جلسة التطبيق الأساسي. تجديد الكود يلغي القديم والجلسات السابقة. إيقاف الموظف أو المسار يمنع الدخول والطلبات؛ إعادة التفعيل تتطلب دخولًا جديدًا. هناك حد إضافي لمحاولات الدخول لكل عنوان IP داخل عملية الخادم؛ عند التوسع لعدة خوادم يمكن نقله لمخزن مشترك.

للموظفين السابقين في ترحيل 0011: استخدم «إصدار كود» من بطاقة المسار؛ الدخول بحساب التطبيق الأساسي وحده لم يعد يمنح صلاحية فريق الفعالية. حقول nurseStaffId/doctorStaffId تسجل الموظف الذي اعتمد القياسات والنصائح، مع إبقاء معرفات الحسابات القديمة عند وجودها.

الأدمن يستطيع إضافة مسار وتعديل اسمه وإيقافه، وإضافة عدة موظفين لكل دور وتجديد أكوادهم وتعطيلهم. لا نحذف المسار أو الموظف حتى تبقى السجلات محفوظة. إدارة عدة فعاليات مستقلة والبوستر والاستبيانات الأخرى لم تُدمج ضمن هذا التسليم.

إدارة الحشود شفهيًا خارج النظام. لم نضف طابورًا إلكترونيًا، أو مسار طوارئ، أو حذفًا للزيارات المنقطعة، أو معالجة تكرار الزيارة وانقطاع الاتصال.

الاستبيان الحالي هو نمط الحياة. أدوات تقييم مخاطر السكري والقلب تحتاج اعتماد النموذج قبل إضافتها؛ لا تُنشأ درجات تقديرية بدل أداة معتمدة.

اعتماد التقرير نهائي في هذه الدورة؛ تحرير تقرير معتمد وإدارة النسخ يحتاجان مسارًا منفصلًا لاحقًا. من أُلغيت صلاحيته لا يمكنه فتح سجل جديد أو حفظ قياسات عبر API.

لم تُطبق الترحيلات على قاعدة البيانات الرسمية ولم يُنشر هذا الفرع على الدومين. اختبارات API تستخدم بيانات تجريبية ومحاكاة طبقة قاعدة البيانات، واختبارات إضافية تفحص شروط SQL لعزل المسارات وانتهاء صلاحية جلسات الفريق، وليست إثباتًا لاتصال قاعدة Manus أو عمل القارئ الفعلي.

## التحقق المنفذ

- `tsc --noEmit`: نجح.
- `vitest run`: نجحت 128 حالة في 22 ملفًا مع `JWT_SECRET` محلي للاختبار فقط. يلزم توفير هذا المتغير عند إعادة الاختبارات، ولا يُستخدم مفتاح الاختبار في الإنتاج.
- `npm run build`: نجح بناء العميل والخادم. ظهرت تنبيهات عن إعدادات analytics غير المتوفرة محليًا وحجم الحزم.
- لم يُختبر القارئ الحقيقي أو تطبيق ترحيل MySQL أو العرض التفاعلي على جوال في هذه البيئة.

## تجربة القبول في بيئة حسن

1. إنشاء مسارين وإضافة طبيب وممرض بكود مستقل لكل مسار.
2. تسجيل مستفيد تجريبي في كل مسار باستخدام رابط التسجيل الخاص به، ثم رفع نتيجة الاختبار الموجودة.
3. الدخول بكود ممرض المسار 1: يفتح المستفيد الأول ولا يفتح الثاني حتى بالبحث بجواله/رقم زيارته.
4. حفظ واعتماد قياساته ثم اعتماد الطبيب للنصائح، والتأكد من وصول التقرير للمستفيد.
5. تكرار الدورة مع إخفاء التمريض للزيارات الجديدة.
6. تجديد كود موظف أثناء دخوله ثم تجربة طلب جديد: يجب رفض الجلسة السابقة والكود القديم. الدخول بالكود الجديد ينجح.
7. إيقاف موظف وإيقاف مسار والتحقق من منع الوصول والتسجيل الجديد؛ إعادة التفعيل ثم الدخول من جديد.
8. اختبار القارئ اللاسلكي على خانة البحث، ثم اختبار البحث بالجوال بدونه.

## إصلاح التسجيل بعد النشر — 26 سبتمبر 2026

- تمت قراءة `events.tracks` من الموقع الرسمي: HTTP 500 عند استعلام `event_tracks`. لا يحدد الرد العام هل السبب جدول ناقص أو اتصال/صلاحية؛ افحص سجل الخادم وقاعدة البيانات المتصلة.
- أزيل طلب قائمة المسارات من شاشة المشارك وشرط تعطيل زر التسجيل المرتبط به. يسند الخادم الرابط العام لأول مسار مفعّل بترتيب رقم المسار؛ روابط المنظم تحدد المسار صراحة. لا يعاد توجيه رابط مسار موقوف إلى مسار آخر.
- هذا التغيير وحده **لا يصلح قاعدة بيانات غير مجهزة**. شغّل `pnpm db:check-events` على نفس DATABASE_URL المستخدم بالخادم. الفحص للقراءة فقط ويتوقف عند نقص الجداول/الأعمدة أو غياب مسار مفعّل.
- عند نقص المخطط، راجع الترحيلات المعلقة 0011 و0012 وطبّقها بـ `pnpm exec drizzle-kit migrate` بعد النسخ الاحتياطي. لا تشغّل generate ولا تحذف بيانات. أعد فحص الجاهزية قبل نشر العميل والخادم معًا.
- اختبار الواجهة القابل للإعادة: شغّل Vite على 5173 ثم `node scripts/test-events-registration.cjs` بعد تثبيت Playwright ومتصفحه. يستخدم واجهة React الفعلية واستجابات API تجريبية معلنة، وليس قاعدة الإنتاج. يختبر الرابط العام ورابط المسار وفشل الحفظ ثم إعادة المحاولة والانتقال للاستبيان بعرض جوال.
- بعد تحديث الإنتاج، يلزم إنشاء زيارة اختبار متفق عليها من `/events` والتأكد من الوصول للاستبيان وحفظ trackId فعلي في قاعدة البيانات؛ الاختبار المحلي لا يغني عن ذلك.

نتيجة التحقق المحلي للإصلاح: 12 اختبار خادم نجحت، وفحص TypeScript والبناء نجحا. نجحت ثلاث حالات متصفح Chromium بعرض 390×844 باستجابات API تجريبية: رابط عام، رابط مسار، وفشل الحفظ ثم إعادة المحاولة. لم تُطبق أي تغييرات على قاعدة بيانات الإنتاج.
